services:
  postgres:
    image: postgres:16-alpine
    container_name: qingzhuo-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: qingzhuo
    ports:
      - "5432:5432"
    volumes:
      - qingzhuo-postgres-data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d qingzhuo"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  qingzhuo-postgres-data:
